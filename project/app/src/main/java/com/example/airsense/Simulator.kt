package com.example.airsense

import android.hardware.Sensor;
import android.hardware.SensorEvent;
import android.hardware.SensorManager;

class Simulator(private val sensorManager: SensorManager, private val data: List<String>, private val resampleRate: Int) {

    private val sensorMap = mutableMapOf<String, Sensor>()


    private fun generateSensorEvent(s: String) {

    }

    private fun runSimulation() {

    }

    private fun resampleRecording() {

    }
}

